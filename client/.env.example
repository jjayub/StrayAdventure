# Example environment variables for client
# Copy this to .env.local for local development

# Server URL - In production, this will be your Railway URL
# Leave empty or use localhost:3000 for local development
VITE_SERVER_URL=http://localhost:3000

# Set to true to run without server connection (single player mode)
VITE_OFFLINE_MODE=false
